# FS_EXP_9.1 – Dockerize a React Application with Multi-Stage Build

## 📘 Overview
This project demonstrates how to containerize a React application using a **multi-stage Docker build** to create a lightweight, production-ready image.

---

## 🧱 Steps to Build & Run

### 1️⃣ Build the Docker image
```bash
docker build -t fs_exp_9.1_react_app.
